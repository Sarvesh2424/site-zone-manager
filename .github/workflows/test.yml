name: Test

on:
    push: 
        branches:
            - "master"

jobs:
    print-job-1:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Job-1"
    print-job:
        runs-on: ubuntu-latest
        needs: 
            - print-job-1
        steps:
            - run: echo "Hello world"
